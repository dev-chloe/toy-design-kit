import { DefaultTheme } from "styled-components";
import type { Colors } from "./types";

const colors: Colors = {
  black: "#000000",
  gray: "#888888",
  white: "#FFFFFF",
  accent: "#FF70B4",
};

const theme: DefaultTheme = {
  colors,
};

export { colors, theme };
