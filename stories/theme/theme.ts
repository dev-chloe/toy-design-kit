import type { Colors, DefaultTheme } from "./types";

const colors: Colors = {
  black: "#000000",
  gray: "#888888",
  white: "#FFFFFF",
  accent: "#FF70B4",
};

const theme: DefaultTheme = {
  colors,
};

export { theme };
